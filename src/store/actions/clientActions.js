import { createAction } from "@reduxjs/toolkit";

export const registerClient = createAction("client/REGISTER_CLIENT");
export const setSearchTermClient = createAction("client/SET_SEARCH_TERM_CLIENT");
